# Image security enforcement example

This example shows how to set image security enforcement on a cluster.

Example: 

```
go run main.go -cluster-id-or-name nameOrID -request-type [ enable | disable ]
```